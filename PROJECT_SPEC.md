# 超高层幕墙夹层钢化玻璃装配准入质量闭环

## 项目目标

面向虚构超高层塔楼幕墙工程，建立玻璃从原片切割完成至单元板块装配准入的单一闭环。系统以锁定任务快照为执行依据，用只追加的“原片—钢化片—夹层成品”材料谱系图保存返制代次，用“炉架层位—逻辑温段”覆盖矩阵验证均质证据，并以整数定点算法处理几何、膜材面积、应力、弓形、温压积分和气泡率。后端采用 Go HTTP 服务与嵌入式关系数据库持久化，事务日志、租约状态、待重试仪器调用和单写终局可在重启后恢复；前端提供真实任务工作台、谱系与覆盖矩阵视图、异常复验页及双人裁定页。生产实现规划为 4—6 个业务包、至少 20 个 Go 生产文件、约 2500 行且控制在 2000—3000 行，不以空声明或无关子系统扩充规模；公开测试至少分布于 4 个测试文件并包含 12 个以上确定性用例。

## 端到端业务流程

1. 设计锁定：录入立面分区、板号、原片熔窑批次、名义厚度、边部方案、整数微米轮廓、孔洞与缺口、钢化程序、均质装架邻接图、中间膜批次、洁净合片窗口、高压釜曲线、检测网格、抽检映射和阈值；服务完成几何与身份校验后生成不可变规则摘要及任务代次。
2. 制造推进：操作者依次完成尺寸边质确认、配片追溯、钢化、表面及边部应力检测、均质装架和热循环、夹层合片、预压、高压釜固化；每一步只能消费已锁定的前置证据和当前代次，并追加新证据而不覆盖历史。
3. 资源与膜材协同：执行合片或炉内工序时，在一个数据库事务中校验逻辑时间、原子扣减整数平方毫米膜材、登记裁切或损耗分录并获取对应设备限时互斥租约；失败时全部回滚，相同操作号同内容返回既有结果，不同内容返回幂等冲突。
4. 仪器采集：应力仪、炉温探头、压力传感器、光学扫描仪及破坏试验设备通过可脚本化适配器提交读数；拒绝、断连、超时或格式错误只形成包含逻辑时间、确定重试序号和下一次逻辑时刻的待重试调用，不推进工序。
5. 异常复验与返制：发现爆片、边崩、应力不足、温段缺口、釜压中断、脱层、气泡、光学畸变或破坏试验失败时，依据锁定的炉架邻接关系、同熔窑批次及同中间膜代次计算、排序并持久化唯一复验集合；返制创建新代次和新谱系节点，旧片、炉温轨迹及检测证据永久可查，旧代次迟到回执被隔离。
6. 终局裁定：材料与工序闭合后，两名不同且具备资格的人员分别复核；仲裁器在并发的装配准入、质量隔离和取消请求之间通过单写终局屏障仅接受一个结论，准入成功时生成唯一可验证凭据。

## 核心组件与职责

1. 幕墙玻璃设计与材料规则目录：管理设计版本、规则摘要、整数微米加工几何、阈值、装架邻接图、检测网格和抽检映射，并提供锁定快照及陈旧摘要检测。
2. 玻璃制造任务及谱系聚合：维护任务代次、工序状态机和只追加材料谱系图，禁止重复板号或原片身份，支持返制新代次、旧代次隔离和历史查询。
3. 膜材守恒与设备租约管理器：通过整数面积分录维护批次守恒池，原子处理领用、裁切、回收、试样及损耗，并管理钢化炉、均质炉架位、洁净合片台、高压釜时窗和检测工位的逻辑时间互斥租约。
4. 钢化均质及夹层固化证据记录器：校验工序依赖、架位和租约，记录钢化、应力、均质、合片、预压及高压釜证据，维护连续阶段前缀、覆盖矩阵和确定整数定点计算结果。
5. 检测复验与终局仲裁器：编排仪器调用、光学及破坏试验，生成唯一复验范围，执行双人复核、代次检查及单写终局裁定。
6. Go HTTP API：提供 JSON API、稳定错误封装和静态前端；前端以 package.json、锁定版本的 lockfile 和确定构建脚本生成任务工作台、谱系图、均质覆盖矩阵、膜材账本、待重试队列和终局裁定页面。

## 领域规则与不变量

1. 锁定快照固定玻璃构造、原片来源、加工几何、各炉程序与装架图、膜批次、时间窗口、检测与抽检方案、阈值及代次；执行请求必须携带匹配的规则摘要，锁定后不得原位修改。
2. 轮廓、孔洞、缺口和边距均使用有符号整数微米；轮廓须非退化且无非法自交，加工区域必须位于允许边界，负值、越界及乘加溢出导致整次请求拒绝。
3. 谱系节点与边只追加，父节点类型只能按原片到钢化片、钢化片到夹层成品推进；同一工程分区内板号唯一，原片身份全局唯一，返制只允许增加代次。
4. 膜材库存使用整数平方毫米复式分录，期初面积等于可用、在制、成品、回收、试样和合理损耗之和；任何分录不平或余额不足均不得提交。
5. 设备资源按资源键和逻辑时间区间互斥；只有有效租约持有人可写证据，过期租约不可续写。膜材扣减与租约获取必须处于同一事务，幂等键同时绑定请求内容摘要。
6. 工序顺序固定为边质确认、钢化、应力检测、均质、合片、预压、釜固化、光学扫描、破坏试验、异常复验、双人复核、终局；均质证据须形成升温—保温—降温连续前缀，高压釜证据须形成预热—升压—保压—降压—冷却连续前缀，样点按逻辑时间严格递增且不得存在超出规则上限的缺口。
7. 表面应力、边部应力、整体弓形、局部波形、升降温速率、保温等效时长、釜压积分和气泡面积率采用规定缩放因子的整数定点运算及统一半远离零舍入；计算前检查符号、除零、时间间隔和乘加溢出。
8. 异常范围由当前锁定邻接图、同原片熔窑批次及同中间膜代次的并集决定，并按立面分区、板号、原片、炉次、架位、检测网格、代次排序去重；范围摘要保证同一异常事实只能生成一个复验集合。
9. 仪器失败不得产生合格读数；待重试调用使用逻辑时钟和固定退避序列，达到锁定重试上限后转为异常，不依赖墙上时间或随机抖动。
10. 准入要求当前代次谱系完整、膜材守恒、全部工序与温压时间轴闭合、应力及外观达标、抽样破坏试验和异常复验完成，并有两名不同合格人员独立复核；准入、隔离或取消一经单写终局提交便不可替换。

## 数据模型与持久化

1. DesignSnapshot：工程、立面分区、构造版本、规则摘要、厚度、边部方案、整数微米轮廓及孔缺口、程序引用、检测网格、抽检映射、阈值和锁定代次。
2. ManufacturingTask、Generation、ProcessEvidence：记录板号、当前代次、工序前缀、证据内容摘要、逻辑时间、操作者、租约引用及不可变完成结果。
3. MaterialNode、LineageEdge：分别表示原片、钢化片、夹层成品及其只追加父子关系，保存熔窑批次、返制代次和失效标记但不物理删除。
4. RackPlan、RackPosition、RackAdjacency、HeatSegment、CoverageCell：保存炉次、架位层位、锁定邻接边、升温/保温/降温样点及每个架位对逻辑温段的覆盖状态。
5. FilmBatch、FilmAccount、FilmEntry：保存膜材代次、整数面积余额及领用、裁切、回收、试样、损耗分录；事务内校验分录总和与非负余额。
6. ResourceLease、IdempotencyRecord：保存资源键、持有者、逻辑起止时刻、状态，以及操作号、请求摘要、响应摘要和提交事务号。
7. InstrumentCall、RetryRecord、Measurement：保存设备类型、脚本化调用结果、逻辑重试次数、格式校验状态及经定点计算的应力、弓形、温压、气泡和光学结果。
8. RetestSet、Review、FinalBarrier、AdmissionCredential：保存异常事实摘要、确定排序的受影响代次、两名复核人及资格快照、唯一终局和仅在准入终局下创建的凭据。
9. 持久化采用支持事务与唯一约束的嵌入式关系数据库；迁移建表后以事务号、未完成租约、待重试调用和当前代次索引恢复状态，重启时不重放已提交幂等结果，不把过期租约恢复为有效。

## 公开接口

1. POST /api/designs/lock：校验并锁定设计和制造规则快照；返回规则摘要、任务代次或稳定的几何、身份、构造错配错误码。
2. POST /api/tasks/{id}/operations：以操作号、规则摘要、代次、逻辑时间和工序载荷推进制造；统一处理膜材、租约、证据追加及幂等冲突。
3. POST /api/tasks/{id}/samples：提交严格有序的炉温或釜压样点和阶段声明；返回当前连续前缀、覆盖矩阵或确定排序的缺口原因。
4. POST /api/tasks/{id}/instrument-calls 与 POST /api/retries/{id}/run：触发脚本化设备调用及到期重试，失败只更新待重试状态。
5. POST /api/tasks/{id}/anomalies：登记异常并一次性生成复验集合；GET /api/tasks/{id}/retests 查询确定排序范围和代次隔离状态。
6. POST /api/tasks/{id}/reviews 与 POST /api/tasks/{id}/verdicts：登记独立复核并竞争写入准入、隔离或取消终局；GET /api/credentials/{id} 查询准入凭据。
7. GET /api/tasks/{id}、/lineage、/coverage、/film-ledger：向前端提供任务状态、材料谱系、均质覆盖矩阵和膜材守恒账本。
8. 所有拒绝使用统一 JSON 结构 {code,message,reasons,operation_id}；code 为稳定常量，reasons 按立面分区、板号、原片、炉次、架位、检测网格、代次排序。
9. 单页前端提供任务锁定表单、工序时间线、可展开谱系、炉架覆盖矩阵、膜材余额与分录、仪器重试队列、复验范围及双人终局操作；前端通过上述 API 读取和提交真实数据，不使用静态演示结果。

## 失败边界

1. 请求事务边界覆盖膜材分录、资源租约、阶段证据、谱系边、幂等记录和准入凭据；任一校验或持久化失败均整体回滚，不留下部分副作用。
2. 数据库唯一约束与比较交换分别防止板号、原片身份、谱系边、架位租约、复验范围和终局重复；并发失败映射为稳定业务错误，而非泄漏数据库错误。
3. 阶段样点乱序、时间缺口、错误架位、错误代次、重复阶段或过期租约在写入有效证据前拒绝；原始无效载荷可进入审计拒绝表但不计入工序前缀。
4. 整数算法集中执行边界检查，任何负值、除零、时间倒退或乘加溢出返回稳定计算错误码，且不得保存截断或饱和值。
5. 仪器适配器与状态推进隔离：拒绝、断连、超时、格式错误只持久化确定性待重试记录；重启后按逻辑时间恢复队列，不自动伪造调用成功。
6. 异常传播以已锁定关系和当前代次的单个数据库快照计算；并发返制或迟到回执若代次不匹配则拒绝，不能改变新代次结论。
7. 启动恢复在对外监听前完成迁移、守恒复核、过期租约关闭、未决终局检查与待重试索引重建；检测到不可恢复的不变量破坏时进入只读故障状态。
8. 错误码至少覆盖构造错配、摘要陈旧、身份重复、几何非法、面积不足、租约冲突或过期、阶段越序、样点缺口、定点溢出、设备失败、复验代次冲突、幂等内容冲突和终局已存在。

## 验收标准

1. 锁定操作一次性固定全部设计、材料、程序、装架、时窗、检测、抽检、阈值和代次信息；陈旧摘要、重复身份或构造错配整体拒绝。
2. 整数微米几何校验可确定识别合法边界、孔缺口越界、退化轮廓、非法相交、负值和乘加溢出，失败不创建任务或谱系。
3. 膜材领用、裁切、回收、试样和损耗在整数面积上始终守恒，且扣减与互斥租约获取并发原子；同号同内容重试幂等，同号异内容稳定冲突。
4. 所有制造步骤仅按锁定依赖推进；均质和高压釜阶段及样点构成连续前缀，错误架位、代次、租约、跳段、重复或时间缺口均不能写入有效证据。
5. 全部质量指标通过有溢出保护的确定整数定点算法计算，并对符号、舍入、除零和时间边界给出精确可重复结果。
6. 脚本化仪器的拒绝、断连、超时和格式错误只产生带逻辑时间及固定重试次数的待重试记录，服务重启后可继续且任务状态不被错误推进。
7. 规定异常按锁定邻接图与材料谱系生成唯一、确定排序的复验集合；返制建立新代次并保留旧证据，旧代次迟到回执不能改变当前结论。
8. 满足全部闭合条件并由两名不同合格人员复核后才生成唯一准入凭据；同步并发的准入、隔离和取消恰有一个终局，重启后结果保持不变。

## 确定性测试场景

1. 几何边界测试：固定整数微米多边形、孔洞和缺口恰好位于允许边距时通过，越界一微米时返回同一稳定错误码。
2. 几何原子拒绝测试：退化轮廓、自交、负坐标和乘加溢出组合请求失败后，不存在任务、快照或谱系节点。
3. 身份与摘要测试：重复板号、重复原片身份、玻璃构造错配和陈旧规则摘要分别产生确定错误及排序原因。
4. 谱系唯一性测试：原片到钢化片再到夹层成品只能追加合法边；重复边幂等，分叉违规被拒，返制保留旧代次。
5. 膜材守恒测试：以固定整数面积执行领用、裁切、回收、试样和损耗，逐笔及最终账户恒等式精确成立。
6. 并发超领与租约竞争测试：同步屏障释放两个事务争抢同一膜材余额和架位，仅一个提交，败者不留扣减或租约。
7. 幂等冲突测试：相同操作号及相同正文重复得到相同事务结果，相同操作号但不同正文返回稳定冲突且不改变状态。
8. 工序依赖测试：在未完成边质、钢化或应力检测时推进后继阶段均失败；合法顺序逐步形成唯一前缀。
9. 均质覆盖测试：使用公开炉架邻接表和升温、保温、降温逻辑序列验证覆盖矩阵；跳段、重复、错误架位、乱序及样点缺口不得生效。
10. 高压釜积分边界测试：用固定压力与逻辑时间序列断言统一舍入后的压力积分、升降压速率和保压时长，覆盖零间隔及溢出拒绝。
11. 租约过期测试：逻辑时钟推进至租约终点后提交证据返回过期错误，且没有有效温段、谱系边或面积扣减。
12. 仪器故障测试：脚本依次返回拒绝、超时、格式错误和成功，断言固定重试序号与逻辑时刻，前三次不推进状态，重启后继续相同序列。
13. 异常传播测试：均质爆片或光学失败依据锁定邻接、同熔窑批次和同膜代次生成去重且精确排序的唯一复验集合。
14. 代次隔离测试：复验后创建新代次，旧代次迟到的合格设备回执被拒，旧炉温轨迹和检测证据仍可查询。
15. 事务回滚测试：在膜材扣减后注入持久化失败，断言膜材余额、租约、有效阶段、谱系边和凭据均无变化。
16. 双人复核测试：同一人员重复复核或无资格人员复核不能满足条件，两名不同合格人员对当前代次复核后才可裁定。
17. 并发终局测试：以同步屏障同时提交准入、隔离和取消，多次运行均只有一个终局和至多一个准入凭据，其他请求返回终局已存在。
18. 重启恢复测试：提交部分工序、有效及过期租约、待重试调用后重启服务，断言已提交证据不重复、过期租约关闭、重试队列与终局状态准确恢复。
19. 确定排序测试：乱序提交多个分区、板号、原片、炉次、架位、网格和代次错误，断言 API reasons 使用规定复合键排序。
20. 前后端连通测试：执行锁定、推进、异常和裁定 API 后加载构建产物，断言页面可取得并展示真实谱系、覆盖矩阵、膜材账本和终局数据。

## 组件追踪关系

1. 规则目录对应验收 1—2，主要由设计锁定、整数几何、身份与摘要公开测试覆盖。
2. 制造任务及谱系聚合对应验收 1、4、7，主要由谱系唯一性、工序依赖、代次隔离和重启恢复测试覆盖。
3. 膜材守恒与设备租约管理器对应验收 3—4，主要由膜材守恒、并发超领、租约竞争、过期与事务回滚测试覆盖。
4. 证据记录器对应验收 4—5，主要由均质覆盖、高压釜积分、阶段依赖和整数溢出边界测试覆盖。
5. 检测复验与终局仲裁器对应验收 6—8，主要由仪器故障、异常传播、双人复核及并发终局测试覆盖。
6. Go HTTP API 与前端贯穿全部验收，负责稳定错误码、确定原因排序、真实页面交互及重启后查询；由确定排序和前后端连通测试覆盖。

## 独特性

本计划聚焦超高层幕墙夹层钢化玻璃从原片切割后到单元板块装配前的制造质量闭环，以只追加三层材料谱系、炉架层位与逻辑温段覆盖矩阵、整数膜材守恒池、温压连续阶段前缀及单写终局屏障为核心，区别于既有食品入线、农业育种、混凝土养护、钢结构卸撑和其他施工移交项目。
